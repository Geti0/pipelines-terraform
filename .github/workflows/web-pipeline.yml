name: Web Pipeline - DISABLED

# This pipeline is DISABLED in favor of AWS CodePipeline
# The CodePipeline handles both infrastructure and web deployment
# See infra/terraform/codepipeline.tf for the new pipeline configuration

on:
  # Commenting out triggers to disable the workflow
  # push:
  #   branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  workflow_dispatch:
    # Only allow manual triggering if needed for testing

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline Disabled Notice
        run: |
          echo "🚫 This GitHub Actions pipeline is DISABLED"
          echo "🚀 AWS CodePipeline is now handling deployments"
          echo "📋 Check the AWS Console for pipeline status"
          echo "🔧 CodePipeline configuration: infra/terraform/codepipeline.tf"
